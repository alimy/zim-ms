module ZimDnsQuery {
    interface DnsQuery {
        // Add add two number
        int Add(int lh,int rh,out int res);

        // Sub subtraction two number
        int Sub(int lh,int rh,out int res);
    };
};
